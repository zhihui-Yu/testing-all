server:
  port: 80

github:
  app:
    client-id: "Iv1.4b6ba24ebd98b055"
    client-secret: "1c35fcdfa7f9a22296328dc92dbdc3d8aec2d413"

# 配置freemarker
spring:
  freemarker:
    # 设置模板后缀名
    suffix: .html # 可以改成 html、ftl
    # 设置文档类型
    content-type: text/html
    # 设置页面编码格式
    charset: UTF-8
    # 设置页面缓存
    cache: false
    # 设置ftl文件路径
    template-loader-path:
      - classpath:/templates
  # 设置静态文件路径，js,css等
  mvc:
    static-path-pattern: /static/**