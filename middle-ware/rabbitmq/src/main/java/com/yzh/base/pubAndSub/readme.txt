## 发布订阅队列使用
一次向许多消费者发送消息

有四种类型:

    fanout: 扇出交换非常简单,它只是将接收到的所有消息广播到它知道的所有队列中。而这正是我们记录器所需要的。

注意：
    1.需要先启动消费者，在启动提供者，提供者会发送给所有交换类型一致的queue


发布者确认：
    等到接受者确认后再尝试发送新消息(可以异步，但是需要一个回调方法)
    可以批量发布消息(当一批处理完发送一个确认，提高吞吐量)
