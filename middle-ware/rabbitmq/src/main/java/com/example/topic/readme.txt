## 话题队列使用
根据模式（主题）接收消息

绑定密钥也必须采用相同的形式。主题交换背后的逻辑类似于直接交换的逻辑，
使用特定路由密钥发送的消息将被传递到所有使用匹配绑定密钥绑定的队列。
但是，绑定键有两个重要的特殊情况：
    *（星号）可以代替一个单词。
    ＃（哈希）可以替代零个或多个单词。

注意：
    1.发送到主题交换的消息不能具有任意的 routing_key-它必须是单词列表，以点分隔。
    2.路由密钥中可以包含任意多个单词，最多255个字节。
    3.如果路由键不匹配则消息会消失
